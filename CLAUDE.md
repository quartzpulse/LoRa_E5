LoRa-E5 Zephyr library lives in modules/lora_e5/ — read modules/lora_e5/CLAUDE.md before working on it.

Always use the project's `.venv` (from the repo root: `source .venv/bin/activate`, or invoke `.venv/bin/west` directly) for every `west` command — `west` is installed there, not system-wide.