# Minimal configuration: the status LED library and its WS2812 backend only.
# No networking/MQTT/OTA -- this sample demonstrates the library in isolation.
CONFIG_STATUS_LED=y
CONFIG_STATUS_LED_BACKEND_WS2812=y

CONFIG_LOG=y
